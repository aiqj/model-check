{
  "name": "model-check",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name model-check"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
